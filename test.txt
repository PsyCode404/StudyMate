<body>
  <h2> ma liste des taches  : </h2>
  <form action"=add" method="post" >
  <label> nom de tache : </label> 
  <input type="text" name="tache" required>
  <input type="submit" value="ajouter">
  </form>
<p>${message}</p>
<c:if not empty listedestache>
  <h3> liste actuelle </h3>
  <ul>  
  <c:foreach var="t" items="listedestache"> 
  <li><${t}>
  <a href="delete?tache=${t}" > supprimer </a>
  </li>

  </c:foreach>
  </ul>


</c:if>
</body>
public class addservlet extend httpservlet {
    protected void doPost (httpservletrequest request , httpservlet response ) throws servleteException ,IOException {
        string tache = request.getparamatre("tache");
        httpsession session = request.getsession(true);
        arrayListe<string >  liste = (ArrayList<String>) session.getAttribute("listeTaches");
        if (liste = null) {
            liste = new  arrayListe<string > (); 

        } 
        if(!liste contains tache){
            liste.add (tache);
            request.setAttribute ("message" , "ajout succeded");
            else {
                request.setattribute("message","existe");

            }; 
            session.setattributte(listetache , liste); 

        }
        requestdispatcher rd = getrequesttdispatcher (index.jsp);
        rd.;forward(request,response);

    }

}